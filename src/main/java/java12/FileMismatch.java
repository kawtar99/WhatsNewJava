package java12;

/**
 * Given two files, the method Files::mismatch should
 * return the position of the first mismatched byte in their contents
 * returns -1 if the content is the same
 */
public class FileMismatch {

    // See test class
}
